#include<bits/stdc++.h>
using namespace std;

int main(){
	// fast i/o
	ios::sync_with_stdio(false);
	cin.tie(NULL);

	// use "\n" instead of endl as \n is faster than endl
	int t;
	cin >> t;
	
	while(t--){

	}
}